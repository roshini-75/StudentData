<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.StudentData" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/your_custom_color_primary</item>
        <item name="android:windowBackground">@color/custom_background_color</item>
    </style>

    <style name="Theme.StudentData" parent="Base.Theme.StudentData" />
</resources>
